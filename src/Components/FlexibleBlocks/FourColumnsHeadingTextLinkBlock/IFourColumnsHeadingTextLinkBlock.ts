// Auto-generated from ACF field group config
export default interface IFourColumnsHeadingTextLinkBlock {
  acf_fc_layout: 'four_columns_heading_text_link';
  four_columns_heading_text_link_fields?: {
    title?: string; // Title (text)
    columns?: {
    heading?: string; // Heading (text)
    text?: string; // Text (wysiwyg)
    link?: { url: string; title?: string; target?: string | null }; // Link (link)
  }[]; // Columns (repeater)
  }; // Four Columns Heading Text Link Fields (group)

}
