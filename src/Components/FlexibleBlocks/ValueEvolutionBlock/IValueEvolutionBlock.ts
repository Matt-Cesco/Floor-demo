// Auto-generated from ACF field group config
export default interface IValueEvolutionBlock {
  acf_fc_layout: 'value_evolution';
  value_evolution_fields?: {
    top_title?: string; // Top Title (text)
    title?: string; // Title (text)
    text?: string; // Text (wysiwyg)
    first_card_title?: string; // First Card Title (text)
    first_card_text?: string; // First Card Text (wysiwyg)
    second_card_top_title?: string; // Second Card Top Title (text)
    second_card_title?: string; // Second Card Title (text)
    second_card_text?: string; // Second Card Text (wysiwyg)
    second_card_cta_link?: { url: string; title?: string; target?: string | null }; // Second Card CTA Link (link)
  }; // Value Evolution Fields (group)

}
