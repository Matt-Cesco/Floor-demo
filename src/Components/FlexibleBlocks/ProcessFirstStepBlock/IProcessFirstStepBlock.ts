// Auto-generated from ACF field group config
export default interface IProcessFirstStepBlock {
  acf_fc_layout: 'process_first_step';
  process_first_step_fields?: {
    top_title?: string; // Top Title (text)
    title?: string; // Title (text)
    cards?: {
    title?: string; // Title (text)
    description?: string; // Description (wysiwyg)
  }[]; // Cards (repeater)
  }; // Process First Step Fields (group)

}
