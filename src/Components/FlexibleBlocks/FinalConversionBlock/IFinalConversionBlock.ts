// Auto-generated from ACF field group config
export default interface IFinalConversionBlock {
  acf_fc_layout: 'final_conversion';
  final_conversion_fields?: {
    top_title?: string; // Top Title (text)
    title?: string; // Title (text)
    description?: string; // Description (wysiwyg)
    left_card_tag?: string; // Left Card Tag (text)
    left_card_title?: string; // Left Card Title (text)
    left_card_description?: string; // Left Card Description (wysiwyg)
    left_card_link?: { url: string; title?: string; target?: string | null }; // Left Card Link (link)
    right_card_tag?: string; // Right Card Tag (text)
    right_card_title?: string; // Right Card Title (text)
    right_card_description?: string; // Right Card Description (wysiwyg)
    right_card_link?: { url: string; title?: string; target?: string | null }; // Right Card Link (link)
  }; // Final Conversion Fields (group)

}
